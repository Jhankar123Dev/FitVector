-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.ai_interviews (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  applicant_id uuid NOT NULL,
  job_post_id uuid NOT NULL,
  interview_type text NOT NULL CHECK (interview_type = ANY (ARRAY['technical'::text, 'behavioral'::text, 'role_specific'::text, 'general'::text])),
  duration_planned integer,
  duration_actual integer,
  status text NOT NULL DEFAULT 'invited'::text CHECK (status = ANY (ARRAY['invited'::text, 'started'::text, 'completed'::text, 'expired'::text, 'cancelled'::text])),
  invite_sent_at timestamp with time zone,
  invite_expires_at timestamp with time zone,
  started_at timestamp with time zone,
  completed_at timestamp with time zone,
  transcript jsonb DEFAULT '[]'::jsonb,
  audio_recording_url text,
  evaluation_report jsonb,
  overall_score integer CHECK (overall_score >= 0 AND overall_score <= 100),
  skill_scores jsonb DEFAULT '[]'::jsonb,
  strengths jsonb DEFAULT '[]'::jsonb,
  concerns jsonb DEFAULT '[]'::jsonb,
  cheating_confidence text CHECK (cheating_confidence = ANY (ARRAY['low'::text, 'medium'::text, 'high'::text])),
  cheating_signals jsonb DEFAULT '[]'::jsonb,
  communication_assessment jsonb,
  ai_recommendation text CHECK (ai_recommendation = ANY (ARRAY['strong_advance'::text, 'advance'::text, 'borderline'::text, 'reject'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_interviews_pkey PRIMARY KEY (id),
  CONSTRAINT ai_interviews_applicant_id_fkey FOREIGN KEY (applicant_id) REFERENCES public.applicants(id),
  CONSTRAINT ai_interviews_job_post_id_fkey FOREIGN KEY (job_post_id) REFERENCES public.job_posts(id)
);
CREATE TABLE public.applicants (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  job_post_id uuid NOT NULL,
  user_id uuid,
  name text NOT NULL,
  email text NOT NULL,
  phone text,
  role_title text,
  current_company text,
  experience integer,
  avatar_url text,
  resume_url text,
  resume_parsed_json jsonb,
  screening_responses jsonb DEFAULT '[]'::jsonb,
  interest_note text,
  source text NOT NULL DEFAULT 'external'::text CHECK (source = ANY (ARRAY['fitvector_organic'::text, 'external_link'::text, 'referral'::text, 'imported'::text, 'boosted'::text])),
  screening_score integer CHECK (screening_score >= 0 AND screening_score <= 100),
  screening_breakdown jsonb,
  screening_summary text,
  bucket text CHECK (bucket = ANY (ARRAY['strong_fit'::text, 'good_fit'::text, 'potential_fit'::text, 'weak_fit'::text])),
  pipeline_stage text NOT NULL DEFAULT 'applied'::text,
  rejection_reason text,
  is_talent_pool boolean DEFAULT false,
  talent_pool_tags ARRAY DEFAULT '{}'::text[],
  is_boosted boolean DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT applicants_pkey PRIMARY KEY (id),
  CONSTRAINT applicants_job_post_id_fkey FOREIGN KEY (job_post_id) REFERENCES public.job_posts(id),
  CONSTRAINT applicants_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.application_boosts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  fitvector_application_id uuid NOT NULL,
  user_id uuid NOT NULL,
  boost_tier text NOT NULL CHECK (boost_tier = ANY (ARRAY['basic'::text, 'standard'::text, 'premium'::text])),
  amount_paid integer NOT NULL,
  currency text DEFAULT 'INR'::text,
  payment_id text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT application_boosts_pkey PRIMARY KEY (id),
  CONSTRAINT application_boosts_fitvector_application_id_fkey FOREIGN KEY (fitvector_application_id) REFERENCES public.fitvector_applications(id),
  CONSTRAINT application_boosts_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.applications (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  job_id uuid,
  job_title text NOT NULL,
  company_name text NOT NULL,
  company_logo_url text,
  location text,
  job_url text,
  job_source USER-DEFINED,
  status USER-DEFINED NOT NULL DEFAULT 'saved'::application_status,
  status_history jsonb DEFAULT '[]'::jsonb,
  tailored_resume_id uuid,
  outreach_ids ARRAY DEFAULT '{}'::uuid[],
  applied_at timestamp with time zone,
  applied_via text,
  contact_name text,
  contact_email text,
  contact_role text,
  interview_rounds jsonb DEFAULT '[]'::jsonb,
  notes text,
  next_followup_date date,
  followup_reminder_sent boolean DEFAULT false,
  salary_offered integer,
  salary_currency text DEFAULT 'INR'::text,
  rejection_reason text,
  offer_details jsonb,
  position_order integer,
  is_archived boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  fitvector_app_id uuid,
  CONSTRAINT applications_pkey PRIMARY KEY (id),
  CONSTRAINT applications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT applications_job_id_fkey FOREIGN KEY (job_id) REFERENCES public.jobs(id),
  CONSTRAINT applications_tailored_resume_id_fkey FOREIGN KEY (tailored_resume_id) REFERENCES public.tailored_resumes(id),
  CONSTRAINT applications_fitvector_app_id_fkey FOREIGN KEY (fitvector_app_id) REFERENCES public.fitvector_applications(id)
);
CREATE TABLE public.assessment_submissions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  assessment_id uuid NOT NULL,
  applicant_id uuid NOT NULL,
  job_post_id uuid NOT NULL,
  status text NOT NULL DEFAULT 'invited'::text CHECK (status = ANY (ARRAY['invited'::text, 'started'::text, 'submitted'::text, 'graded'::text, 'expired'::text])),
  invited_at timestamp with time zone,
  started_at timestamp with time zone,
  submitted_at timestamp with time zone,
  graded_at timestamp with time zone,
  time_taken_minutes integer,
  answers jsonb DEFAULT '[]'::jsonb,
  auto_score integer CHECK (auto_score >= 0 AND auto_score <= 100),
  manual_score integer CHECK (manual_score >= 0 AND manual_score <= 100),
  final_score integer CHECK (final_score >= 0 AND final_score <= 100),
  grader_id uuid,
  grader_notes text,
  plagiarism_flag boolean DEFAULT false,
  proctoring_flags jsonb DEFAULT '[]'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  expires_at timestamp with time zone,
  CONSTRAINT assessment_submissions_pkey PRIMARY KEY (id),
  CONSTRAINT assessment_submissions_assessment_id_fkey FOREIGN KEY (assessment_id) REFERENCES public.assessments(id),
  CONSTRAINT assessment_submissions_applicant_id_fkey FOREIGN KEY (applicant_id) REFERENCES public.applicants(id),
  CONSTRAINT assessment_submissions_job_post_id_fkey FOREIGN KEY (job_post_id) REFERENCES public.job_posts(id),
  CONSTRAINT assessment_submissions_grader_id_fkey FOREIGN KEY (grader_id) REFERENCES public.users(id)
);
CREATE TABLE public.assessments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  created_by uuid NOT NULL,
  name text NOT NULL,
  assessment_type text NOT NULL CHECK (assessment_type = ANY (ARRAY['coding_test'::text, 'mcq_quiz'::text, 'mixed'::text, 'case_study'::text, 'assignment'::text])),
  time_limit_minutes integer,
  difficulty text CHECK (difficulty = ANY (ARRAY['easy'::text, 'medium'::text, 'hard'::text])),
  passing_score integer CHECK (passing_score >= 0 AND passing_score <= 100),
  questions jsonb DEFAULT '[]'::jsonb,
  settings jsonb DEFAULT '{}'::jsonb,
  is_template boolean DEFAULT false,
  times_used integer DEFAULT 0,
  status text DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'archived'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT assessments_pkey PRIMARY KEY (id),
  CONSTRAINT assessments_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT assessments_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.boost_credits (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  credits_remaining integer NOT NULL DEFAULT 0,
  credits_purchased integer NOT NULL DEFAULT 0,
  amount_paid integer NOT NULL,
  currency text DEFAULT 'INR'::text,
  payment_id text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT boost_credits_pkey PRIMARY KEY (id),
  CONSTRAINT boost_credits_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.candidate_notes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  applicant_id uuid NOT NULL,
  author_id uuid NOT NULL,
  body text NOT NULL,
  mentions ARRAY DEFAULT '{}'::uuid[],
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT candidate_notes_pkey PRIMARY KEY (id),
  CONSTRAINT candidate_notes_applicant_id_fkey FOREIGN KEY (applicant_id) REFERENCES public.applicants(id),
  CONSTRAINT candidate_notes_author_id_fkey FOREIGN KEY (author_id) REFERENCES public.users(id)
);
CREATE TABLE public.candidate_votes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  applicant_id uuid NOT NULL,
  voter_id uuid NOT NULL,
  vote text NOT NULL CHECK (vote = ANY (ARRAY['strong_hire'::text, 'hire'::text, 'no_hire'::text, 'strong_no_hire'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT candidate_votes_pkey PRIMARY KEY (id),
  CONSTRAINT candidate_votes_applicant_id_fkey FOREIGN KEY (applicant_id) REFERENCES public.applicants(id),
  CONSTRAINT candidate_votes_voter_id_fkey FOREIGN KEY (voter_id) REFERENCES public.users(id)
);
CREATE TABLE public.community_comments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  post_id uuid NOT NULL,
  user_id uuid NOT NULL,
  parent_comment_id uuid,
  body text NOT NULL,
  is_anonymous boolean DEFAULT false,
  upvotes integer DEFAULT 0,
  status text DEFAULT 'published'::text CHECK (status = ANY (ARRAY['published'::text, 'flagged'::text, 'removed'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT community_comments_pkey PRIMARY KEY (id),
  CONSTRAINT community_comments_post_id_fkey FOREIGN KEY (post_id) REFERENCES public.community_posts(id),
  CONSTRAINT community_comments_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT community_comments_parent_comment_id_fkey FOREIGN KEY (parent_comment_id) REFERENCES public.community_comments(id)
);
CREATE TABLE public.community_posts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  post_type text NOT NULL CHECK (post_type = ANY (ARRAY['interview_experience'::text, 'discussion'::text, 'salary_report'::text])),
  title text NOT NULL,
  body text NOT NULL,
  category text CHECK (category = ANY (ARRAY['tech'::text, 'business'::text, 'design'::text, 'marketing'::text, 'career_advice'::text, 'salary'::text, 'general'::text])),
  is_anonymous boolean DEFAULT true,
  upvotes integer DEFAULT 0,
  downvotes integer DEFAULT 0,
  comments_count integer DEFAULT 0,
  status text DEFAULT 'published'::text CHECK (status = ANY (ARRAY['published'::text, 'flagged'::text, 'removed'::text, 'under_review'::text])),
  interview_data jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT community_posts_pkey PRIMARY KEY (id),
  CONSTRAINT community_posts_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.community_votes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  target_type text NOT NULL CHECK (target_type = ANY (ARRAY['post'::text, 'comment'::text])),
  target_id uuid NOT NULL,
  vote_type text NOT NULL CHECK (vote_type = ANY (ARRAY['up'::text, 'down'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT community_votes_pkey PRIMARY KEY (id),
  CONSTRAINT community_votes_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.companies (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  logo_url text,
  website_url text,
  industry text,
  company_size text CHECK (company_size = ANY (ARRAY['1-10'::text, '11-50'::text, '51-200'::text, '201-1000'::text, '1000+'::text])),
  description text,
  culture_keywords ARRAY DEFAULT '{}'::text[],
  locations jsonb DEFAULT '[]'::jsonb,
  branding jsonb DEFAULT '{}'::jsonb,
  created_by uuid,
  plan_tier text NOT NULL DEFAULT 'starter'::text CHECK (plan_tier = ANY (ARRAY['starter'::text, 'growth'::text, 'business'::text, 'enterprise'::text])),
  plan_expiry timestamp with time zone,
  plan_payment_id text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_transparent_pipeline boolean NOT NULL DEFAULT false,
  CONSTRAINT companies_pkey PRIMARY KEY (id),
  CONSTRAINT companies_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.company_members (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  user_id uuid NOT NULL,
  role text NOT NULL CHECK (role = ANY (ARRAY['admin'::text, 'recruiter'::text, 'hiring_manager'::text, 'viewer'::text])),
  invited_by uuid,
  invite_email text,
  status text NOT NULL DEFAULT 'invited'::text CHECK (status = ANY (ARRAY['invited'::text, 'active'::text, 'deactivated'::text])),
  invited_at timestamp with time zone NOT NULL DEFAULT now(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT company_members_pkey PRIMARY KEY (id),
  CONSTRAINT company_members_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT company_members_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT company_members_invited_by_fkey FOREIGN KEY (invited_by) REFERENCES public.users(id)
);
CREATE TABLE public.employer_usage (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  action_type text NOT NULL CHECK (action_type = ANY (ARRAY['resume_screen'::text, 'ai_interview'::text, 'assessment'::text, 'job_post'::text])),
  month_year text NOT NULL,
  count integer DEFAULT 0,
  CONSTRAINT employer_usage_pkey PRIMARY KEY (id),
  CONSTRAINT employer_usage_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.fitvector_applications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  applicant_user_id uuid NOT NULL,
  job_post_id uuid NOT NULL,
  applicant_id uuid,
  tailored_resume_id uuid,
  match_score integer CHECK (match_score >= 0 AND match_score <= 100),
  screening_responses jsonb DEFAULT '[]'::jsonb,
  interest_note text,
  resume_name text,
  is_boosted boolean DEFAULT false,
  boost_tier text CHECK (boost_tier = ANY (ARRAY['basic'::text, 'standard'::text, 'premium'::text])),
  status text NOT NULL DEFAULT 'applied'::text CHECK (status = ANY (ARRAY['applied'::text, 'under_review'::text, 'interview_invited'::text, 'interviewed'::text, 'decision_pending'::text, 'offered'::text, 'rejected'::text, 'withdrawn'::text])),
  status_timeline jsonb DEFAULT '[]'::jsonb,
  status_updated_at timestamp with time zone,
  employer_notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT fitvector_applications_pkey PRIMARY KEY (id),
  CONSTRAINT fitvector_applications_applicant_user_id_fkey FOREIGN KEY (applicant_user_id) REFERENCES public.users(id),
  CONSTRAINT fitvector_applications_job_post_id_fkey FOREIGN KEY (job_post_id) REFERENCES public.job_posts(id),
  CONSTRAINT fitvector_applications_applicant_id_fkey FOREIGN KEY (applicant_id) REFERENCES public.applicants(id),
  CONSTRAINT fitvector_applications_tailored_resume_id_fkey FOREIGN KEY (tailored_resume_id) REFERENCES public.tailored_resumes(id)
);
CREATE TABLE public.generated_outreach (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  job_id uuid,
  outreach_type USER-DEFINED NOT NULL,
  subject text,
  subject_alternatives ARRAY,
  body text NOT NULL,
  tone text DEFAULT 'professional'::text,
  recruiter_name text,
  recruiter_email text,
  recruiter_title text,
  email_confidence USER-DEFINED,
  job_title text,
  company_name text,
  personalization_points ARRAY,
  was_copied boolean DEFAULT false,
  was_sent boolean DEFAULT false,
  ai_model text DEFAULT 'gemini-2.0-flash'::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT generated_outreach_pkey PRIMARY KEY (id),
  CONSTRAINT generated_outreach_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT generated_outreach_job_id_fkey FOREIGN KEY (job_id) REFERENCES public.jobs(id)
);
CREATE TABLE public.human_interviews (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  applicant_id uuid NOT NULL,
  job_post_id uuid NOT NULL,
  interviewer_id uuid NOT NULL,
  round_number integer NOT NULL,
  interview_type text CHECK (interview_type = ANY (ARRAY['phone_screen'::text, 'technical'::text, 'behavioral'::text, 'culture_fit'::text, 'hiring_manager'::text, 'panel'::text])),
  scheduled_at timestamp with time zone,
  duration_minutes integer DEFAULT 60,
  meeting_link text,
  calendar_event_id text,
  status text NOT NULL DEFAULT 'scheduled'::text CHECK (status = ANY (ARRAY['scheduled'::text, 'completed'::text, 'cancelled'::text, 'rescheduled'::text, 'no_show'::text])),
  feedback jsonb,
  rating text CHECK (rating = ANY (ARRAY['strong_hire'::text, 'hire'::text, 'no_hire'::text, 'strong_no_hire'::text])),
  notes text,
  rescheduled_from timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT human_interviews_pkey PRIMARY KEY (id),
  CONSTRAINT human_interviews_applicant_id_fkey FOREIGN KEY (applicant_id) REFERENCES public.applicants(id),
  CONSTRAINT human_interviews_job_post_id_fkey FOREIGN KEY (job_post_id) REFERENCES public.job_posts(id),
  CONSTRAINT human_interviews_interviewer_id_fkey FOREIGN KEY (interviewer_id) REFERENCES public.users(id)
);
CREATE TABLE public.interview_panels (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  name text NOT NULL,
  interview_type text NOT NULL CHECK (interview_type = ANY (ARRAY['technical'::text, 'behavioral'::text, 'hr'::text, 'culture_fit'::text, 'final'::text])),
  default_interviewer_ids ARRAY DEFAULT '{}'::uuid[],
  round_number integer NOT NULL DEFAULT 1,
  duration_minutes integer NOT NULL DEFAULT 60,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT interview_panels_pkey PRIMARY KEY (id),
  CONSTRAINT interview_panels_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.interview_participants (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  human_interview_id uuid NOT NULL,
  user_id uuid NOT NULL,
  role text NOT NULL DEFAULT 'interviewer'::text CHECK (role = ANY (ARRAY['lead'::text, 'interviewer'::text, 'shadow'::text, 'hiring_manager'::text])),
  response_status text NOT NULL DEFAULT 'needsAction'::text CHECK (response_status = ANY (ARRAY['needsAction'::text, 'accepted'::text, 'declined'::text, 'tentative'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT interview_participants_pkey PRIMARY KEY (id),
  CONSTRAINT interview_participants_human_interview_id_fkey FOREIGN KEY (human_interview_id) REFERENCES public.human_interviews(id),
  CONSTRAINT interview_participants_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.job_matches (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  job_id uuid NOT NULL,
  match_score smallint NOT NULL CHECK (match_score >= 0 AND match_score <= 100),
  match_bucket USER-DEFINED NOT NULL,
  decision_label USER-DEFINED NOT NULL DEFAULT 'explore'::decision_label,
  similarity_raw real,
  embedding_score smallint,
  deterministic_score smallint,
  deterministic_components jsonb,
  gap_analysis jsonb,
  gap_analysis_generated_at timestamp with time zone,
  is_seen boolean NOT NULL DEFAULT false,
  is_saved boolean NOT NULL DEFAULT false,
  is_dismissed boolean NOT NULL DEFAULT false,
  seen_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT job_matches_pkey PRIMARY KEY (id),
  CONSTRAINT job_matches_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT job_matches_job_id_fkey FOREIGN KEY (job_id) REFERENCES public.jobs(id)
);
CREATE TABLE public.job_posts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  created_by uuid NOT NULL,
  title text NOT NULL,
  department text,
  location text,
  is_remote boolean DEFAULT false,
  work_mode text CHECK (work_mode = ANY (ARRAY['onsite'::text, 'remote'::text, 'hybrid'::text])),
  job_type text CHECK (job_type = ANY (ARRAY['fulltime'::text, 'parttime'::text, 'contract'::text, 'internship'::text])),
  experience_min integer,
  experience_max integer,
  salary_min integer,
  salary_max integer,
  salary_currency text DEFAULT 'INR'::text,
  salary_visible boolean DEFAULT true,
  description text NOT NULL,
  required_skills ARRAY DEFAULT '{}'::text[],
  nice_to_have_skills ARRAY DEFAULT '{}'::text[],
  screening_questions jsonb DEFAULT '[]'::jsonb,
  openings_count integer DEFAULT 1,
  application_deadline timestamp with time zone,
  interview_plan jsonb,
  assessment_config jsonb,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'active'::text, 'paused'::text, 'closed'::text, 'filled'::text])),
  auto_advance_threshold integer CHECK (auto_advance_threshold >= 0 AND auto_advance_threshold <= 100),
  auto_reject_threshold integer CHECK (auto_reject_threshold >= 0 AND auto_reject_threshold <= 100),
  dimension_weights jsonb,
  views_count integer DEFAULT 0,
  applications_count integer DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  pipeline_stages ARRAY NOT NULL DEFAULT ARRAY['applied'::text, 'ai_screened'::text, 'assessment_pending'::text, 'assessment_completed'::text, 'ai_interview_pending'::text, 'ai_interviewed'::text, 'human_interview'::text, 'interview_scheduled'::text, 'offer'::text, 'hired'::text],
  assessment_id uuid,
  CONSTRAINT job_posts_pkey PRIMARY KEY (id),
  CONSTRAINT job_posts_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT job_posts_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT job_posts_assessment_id_fkey FOREIGN KEY (assessment_id) REFERENCES public.assessments(id)
);
CREATE TABLE public.job_promotions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  job_post_id uuid NOT NULL,
  job_title text NOT NULL,
  promotion_type text NOT NULL CHECK (promotion_type = ANY (ARRAY['sponsored_feed'::text, 'priority_search'::text])),
  duration integer NOT NULL,
  start_date timestamp with time zone NOT NULL,
  end_date timestamp with time zone NOT NULL,
  amount_paid integer NOT NULL,
  currency text NOT NULL DEFAULT 'INR'::text,
  impressions integer NOT NULL DEFAULT 0,
  clicks integer NOT NULL DEFAULT 0,
  applications integer NOT NULL DEFAULT 0,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'expired'::text, 'paused'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT job_promotions_pkey PRIMARY KEY (id),
  CONSTRAINT job_promotions_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT job_promotions_job_post_id_fkey FOREIGN KEY (job_post_id) REFERENCES public.job_posts(id)
);
CREATE TABLE public.jobs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  external_id text,
  source USER-DEFINED NOT NULL DEFAULT 'direct'::job_source,
  sources ARRAY DEFAULT '{}'::job_source[],
  fingerprint text NOT NULL,
  url text NOT NULL,
  title text NOT NULL,
  company_name text NOT NULL,
  company_logo_url text,
  company_url text,
  location text,
  city text,
  state text,
  country text DEFAULT 'India'::text,
  work_mode USER-DEFINED,
  job_type USER-DEFINED,
  description text NOT NULL,
  description_hash text,
  skills_required ARRAY DEFAULT '{}'::text[],
  skills_nice_to_have ARRAY DEFAULT '{}'::text[],
  experience_min integer,
  experience_max integer,
  education_required text,
  salary_min integer,
  salary_max integer,
  salary_currency text DEFAULT 'INR'::text,
  salary_period text DEFAULT 'yearly'::text,
  salary_source text,
  company_size text,
  company_industry text,
  company_rating numeric,
  company_description text,
  embedding USER-DEFINED,
  embedding_model text DEFAULT 'all-MiniLM-L6-v2'::text,
  is_active boolean NOT NULL DEFAULT true,
  posted_at timestamp with time zone,
  scraped_at timestamp with time zone NOT NULL DEFAULT now(),
  last_verified_at timestamp with time zone,
  deactivated_at timestamp with time zone,
  is_easy_apply boolean DEFAULT false,
  apply_url text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  job_post_id uuid,
  posted_by_employer_id uuid,
  CONSTRAINT jobs_pkey PRIMARY KEY (id),
  CONSTRAINT jobs_job_post_id_fkey FOREIGN KEY (job_post_id) REFERENCES public.job_posts(id),
  CONSTRAINT jobs_posted_by_employer_id_fkey FOREIGN KEY (posted_by_employer_id) REFERENCES public.users(id)
);
CREATE TABLE public.notification_log (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  notification_type text NOT NULL,
  channel text NOT NULL,
  title text NOT NULL,
  body text,
  reference_id uuid,
  is_read boolean NOT NULL DEFAULT false,
  sent_at timestamp with time zone NOT NULL DEFAULT now(),
  read_at timestamp with time zone,
  fitvector_application_id uuid,
  CONSTRAINT notification_log_pkey PRIMARY KEY (id),
  CONSTRAINT notification_log_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT fk_notification_fv_app FOREIGN KEY (fitvector_application_id) REFERENCES public.fitvector_applications(id)
);
CREATE TABLE public.notifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  type text NOT NULL DEFAULT 'general'::text CHECK (type = ANY (ARRAY['status_change'::text, 'interview_invite'::text, 'offer'::text, 'rejection'::text, 'general'::text])),
  title text NOT NULL,
  message text NOT NULL,
  is_read boolean NOT NULL DEFAULT false,
  action_url text DEFAULT '#'::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT notifications_pkey PRIMARY KEY (id),
  CONSTRAINT notifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.profile_verifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  category text NOT NULL CHECK (category = ANY (ARRAY['identity'::text, 'education'::text, 'employment'::text, 'skills'::text])),
  status text NOT NULL DEFAULT 'not_started'::text CHECK (status = ANY (ARRAY['not_started'::text, 'pending'::text, 'verified'::text, 'expired'::text])),
  document_url text,
  document_name text,
  submitted_at timestamp with time zone,
  verified_at timestamp with time zone,
  expires_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT profile_verifications_pkey PRIMARY KEY (id),
  CONSTRAINT profile_verifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.promoted_listings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  job_post_id uuid NOT NULL,
  company_id uuid NOT NULL,
  promotion_type text NOT NULL CHECK (promotion_type = ANY (ARRAY['sponsored_feed'::text, 'priority_search'::text])),
  duration_days integer NOT NULL CHECK (duration_days = ANY (ARRAY[7, 14, 30])),
  start_date date NOT NULL,
  end_date date NOT NULL,
  amount_paid integer NOT NULL,
  currency text DEFAULT 'INR'::text,
  payment_id text,
  impressions integer DEFAULT 0,
  clicks integer DEFAULT 0,
  applications integer DEFAULT 0,
  status text DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'expired'::text, 'paused'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT promoted_listings_pkey PRIMARY KEY (id),
  CONSTRAINT promoted_listings_job_post_id_fkey FOREIGN KEY (job_post_id) REFERENCES public.job_posts(id),
  CONSTRAINT promoted_listings_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.question_bank (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  title text NOT NULL,
  category text NOT NULL,
  difficulty text NOT NULL CHECK (difficulty = ANY (ARRAY['easy'::text, 'medium'::text, 'hard'::text])),
  prompt text NOT NULL,
  starter_code jsonb NOT NULL DEFAULT '{}'::jsonb,
  solution_code jsonb NOT NULL DEFAULT '{}'::jsonb,
  test_cases jsonb NOT NULL DEFAULT '[]'::jsonb,
  tags ARRAY DEFAULT '{}'::text[],
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT question_bank_pkey PRIMARY KEY (id)
);
CREATE TABLE public.recruiter_emails (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  company_name text NOT NULL,
  company_domain text,
  recruiter_name text,
  recruiter_email text NOT NULL,
  recruiter_title text,
  confidence USER-DEFINED NOT NULL,
  source text,
  verified_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT recruiter_emails_pkey PRIMARY KEY (id)
);
CREATE TABLE public.salary_reports (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  role_title text NOT NULL,
  company_name text,
  company_size text,
  location text NOT NULL,
  experience_years integer NOT NULL,
  base_salary integer NOT NULL,
  total_compensation integer,
  currency text DEFAULT 'INR'::text,
  is_verified boolean DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT salary_reports_pkey PRIMARY KEY (id),
  CONSTRAINT salary_reports_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.tailored_resumes (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  job_id uuid,
  version_name text NOT NULL,
  template_id text NOT NULL DEFAULT 'modern'::text,
  latex_source text NOT NULL,
  pdf_url text,
  job_title text,
  company_name text,
  ai_model text DEFAULT 'gemini-2.0-flash'::text,
  prompt_version text DEFAULT 'v1'::text,
  generation_time_ms integer,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tailored_resumes_pkey PRIMARY KEY (id),
  CONSTRAINT tailored_resumes_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT tailored_resumes_job_id_fkey FOREIGN KEY (job_id) REFERENCES public.jobs(id)
);
CREATE TABLE public.talent_pool_cache (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  job_post_id uuid NOT NULL,
  results jsonb NOT NULL DEFAULT '[]'::jsonb,
  filters jsonb NOT NULL DEFAULT '{}'::jsonb,
  cached_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT talent_pool_cache_pkey PRIMARY KEY (id),
  CONSTRAINT talent_pool_cache_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT talent_pool_cache_job_post_id_fkey FOREIGN KEY (job_post_id) REFERENCES public.job_posts(id)
);
CREATE TABLE public.usage_logs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  feature text NOT NULL,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT usage_logs_pkey PRIMARY KEY (id),
  CONSTRAINT usage_logs_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.user_profiles (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL UNIQUE,
  current_role text,
  current_company text,
  current_status text,
  experience_level USER-DEFINED,
  target_roles ARRAY DEFAULT '{}'::text[],
  target_locations ARRAY DEFAULT '{}'::text[],
  preferred_work_mode USER-DEFINED,
  preferred_job_types ARRAY DEFAULT '{fulltime}'::job_type[],
  preferred_industries ARRAY DEFAULT '{}'::text[],
  expected_salary_min integer,
  expected_salary_max integer,
  salary_currency text DEFAULT 'INR'::text,
  parsed_resume_json jsonb,
  raw_resume_url text,
  raw_resume_filename text,
  resume_parsed_at timestamp with time zone,
  skills ARRAY DEFAULT '{}'::text[],
  embedding USER-DEFINED,
  embedding_model text DEFAULT 'all-MiniLM-L6-v2'::text,
  embedding_updated_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  phone text,
  linkedin_url text,
  portfolio_url text,
  work_history jsonb NOT NULL DEFAULT '[]'::jsonb,
  CONSTRAINT user_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT user_profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.user_reputation (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  karma_points integer DEFAULT 0,
  helpful_reviews_count integer DEFAULT 0,
  interview_experiences_count integer DEFAULT 0,
  community_posts_count integer DEFAULT 0,
  badges jsonb DEFAULT '[]'::jsonb,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT user_reputation_pkey PRIMARY KEY (id),
  CONSTRAINT user_reputation_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.users (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  email text NOT NULL UNIQUE,
  full_name text NOT NULL DEFAULT ''::text,
  avatar_url text,
  auth_provider USER-DEFINED NOT NULL DEFAULT 'credentials'::auth_provider,
  auth_provider_id text,
  password_hash text,
  email_verified boolean NOT NULL DEFAULT false,
  plan_tier USER-DEFINED NOT NULL DEFAULT 'free'::plan_tier,
  plan_expiry timestamp with time zone,
  plan_payment_id text,
  status USER-DEFINED NOT NULL DEFAULT 'onboarding'::user_status,
  onboarding_completed boolean NOT NULL DEFAULT false,
  notification_preferences jsonb DEFAULT '{"email_digest": "daily", "push_reminders": true, "push_new_matches": true, "push_status_updates": true}'::jsonb,
  last_login_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  role text NOT NULL DEFAULT 'seeker'::text CHECK (role = ANY (ARRAY['seeker'::text, 'employer'::text, 'superadmin'::text])),
  google_refresh_token text,
  google_calendar_connected boolean NOT NULL DEFAULT false,
  CONSTRAINT users_pkey PRIMARY KEY (id)
);
CREATE TABLE public.verified_profiles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  identity_verified boolean DEFAULT false,
  identity_document_ref text,
  education_verified boolean DEFAULT false,
  education_document_ref text,
  employment_verified boolean DEFAULT false,
  employment_document_ref text,
  skills_verified boolean DEFAULT false,
  skills_assessment_id uuid,
  verified_at timestamp with time zone,
  expires_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT verified_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT verified_profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT verified_profiles_skills_assessment_id_fkey FOREIGN KEY (skills_assessment_id) REFERENCES public.assessments(id)
);